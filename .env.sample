# INCA Self-Contained Configuration
# Customize these values as needed for your environment

# Certificate Authority Configuration
CA_NAME=example.org
CA_COUNTRY=US
CA_STATE=California
CA_CITY=San Francisco
CA_ORG=Example Organization
CA_OU=IT Department
CA_EMAIL=admin@example.org
CA_VALIDITY_DAYS=3650

# MinIO Configuration (S3-compatible storage)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# INCA Server Configuration
INCA_HOST=0.0.0.0
INCA_PORT=8080